#ifndef MATRIX_PARSER_H
#define MATRIX_PARSER_H

// Reads a 1D matrix from a file and returns a pointer to array
int* read_matrix_1d(const char* filename, int rows, int cols);

#endif // MATRIX_PARSER_H
